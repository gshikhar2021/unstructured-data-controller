apiVersion: v1
kind: Secret
metadata:
  name: nomic-embedding-api-key
  namespace: unstructured-controller-namespace
type: Opaque
stringData:
  api-key: "YOUR_ACCESS_TOKEN_HERE"  # Replace with your actual access token from the model
