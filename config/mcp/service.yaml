apiVersion: v1
kind: Service
metadata:
  name: unstructured-data-mcp-server
  labels:
    app: unstructured-data-mcp-server
spec:
  type: ClusterIP
  selector:
    app: unstructured-data-mcp-server
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
